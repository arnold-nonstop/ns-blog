<%= form_with model: [post, Comment.new] do |form| %>
  <%= form.label :content, "Your Comment:" %>
  <%= form.text_area :content, size: "20x5", class: "form-control"%>
  <%= form.submit "Add Comment", class: "btn btn-primary mt-4" %>
<% end %>
